//
// Created by bpoumeau on 3/11/23.
//

#include "mini_parsing.h"

int	next_control_operator_is_a_pipe(t_token *tok)
{
	while (tok->token != EOL && tok->token != AMPERSAND && tok->token)
	{
		if (
		tok = tok->next;
	}
}
